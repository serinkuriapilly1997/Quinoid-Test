<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.quinoidtestapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuinoidTestApplication"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".view.ui.profile.ProfileActivity"
            android:theme="@style/Theme.QuinoidTestApplication.NoActionBar">

        </activity>
        <activity
            android:name=".view.ui.detail.DetailActivity"
            android:launchMode="singleTop"
            android:theme="@style/Theme.QuinoidTestApplication.NoActionBar" />
        <activity
            android:name=".view.ui.home.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuinoidTestApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>